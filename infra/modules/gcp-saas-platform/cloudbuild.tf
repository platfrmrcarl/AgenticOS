# Cloud Build trigger intentionally not managed here.
# Each deployment of this module has unique substitutions (URLs, keys, etc.)
# that vary per tenant. Create triggers manually or via your own automation.
#
# The Cloud Build service account (agenticos-cloudbuild@ or <app_name>-cloudbuild@)
# is created in iam.tf and has the necessary roles to build and deploy.
